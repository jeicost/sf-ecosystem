// TODO: Fix import path issues in build. Dev server works fine.
// This page is temporarily disabled for build compatibility.
// The Brand Brain feature (brand-brain page) is fully functional.

export default function AgentPage() {
  return (
    <div style={{ padding: '20px', textAlign: 'center' }}>
      <h1>Agent Page (In Development)</h1>
      <p>This page is temporarily disabled. Please use /brand-brain instead.</p>
    </div>
  )
}
