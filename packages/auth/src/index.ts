// Shared Supabase Auth Helpers
// Each app imports from @sf/auth for consistent authentication patterns

export * from '@supabase/ssr';

// TODO: Add app-specific auth wrappers (session helpers, middleware, etc.)
