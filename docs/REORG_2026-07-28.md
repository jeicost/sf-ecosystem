# Reorganización de proyectos locales — 2026-07-28

Auditoría profunda + reorganización ejecutada con decisiones del CEO registradas en sesión. Verificación previa contra las BD de producción de MIRA (`nnevhtfxuawexliwlbmh`) y del CMS (`dmzecrlkclocqaywkjtc`).

## Espacio recuperado

| Zona | Antes | Después |
|---|---|---|
| Monorepo `Claude.nosync` | 16 GB | ~8.6 GB |
| `~/.claude/projects` | 2.1 GB | 541 MB |

Total: **~9 GB**. Lo grande: `apps/mira/portal/.next` (5.6 GB, caché inflada), `.turbo/` (1.7 GB), historial de sesiones duplicado por el symlink `Desktop/Claude` (1.5 GB).

## Qué se hizo

**Espacio (regenerable):** borrados `.next` de mira/portal, `.turbo/`, carpetas fantasma de macOS (`packages/{auth,supabase,ui} 2`), `projects/forma` (vacía), `.DS_Store`, `vercel.json` raíz vacío.

**Dadybox:** decisión CEO — los documentos antiguos ya no importan; NO se migran a MIRA. En `clients/dadybox/output/` se conservó la última versión de cada entregable y se borraron ~10 variantes timestamped (recuperables en historial git). En MIRA, Dadybox conserva su capa de marca (Brand Bible, 4 pilares, manuales).

**BD MIRA:** borrados los 2 clientes draft vacíos (`draft-vjartfp2`, `draft-b7x8ocr9`) por IDs exactos, con verificación antes (0 contenido en 9 tablas + solo stub de brand_profiles) y después (los 6 clientes reales intactos: salsa-burgers, dadybox, discoolver, startup-factory, nc-global-assets, adrian-grooves).

**NC Global:** la SPA Vite legacy (que sirve ncglobalassets.com hoy vía Vercel `nc-global-assets`) se movió a `clients/_archive/nc-global-assets-vite`. El deploy en Vercel NO depende de la carpeta local — producción sigue intacta. Registry actualizado.

**dg-editor:** consolidado en una sola copia — el repo del Desktop `~/Desktop/discoolver-dg-editor` (git + remote `discoolver-group`). La copia sin git del monorepo (`clients/discoolver/dg-editor`, byte-idéntica, 42 MB) se eliminó. Venv recreado en el Desktop y verificado (uvicorn responde 200 en `/` y `/editor`). Skill `repaso-dg` y lanzador `🚀 Abrir Guías Discoolver.command` reapuntados.

**sf-links:** descatalogado (decisión CEO). La carpeta estaba vacía — el código se perdió con el submódulo fantasma (se eliminó también el gitlink roto del índice). Dominio links.startupsfactory.es queda libre. Registry marcado.

**content-pillars:** el paquete suelto del Desktop (`package to create the content pillars`) ahora vive versionado en `tools/content-pillars/`.

**Higiene git:** `.gitignore` amplía `*.tsbuildinfo`, `.DS_Store`, `clients/dadybox/output/`; destrackeados los 3 `tsconfig.tsbuildinfo`. Ninguna rama local estaba mergeada en main — no se borró ninguna.

**Raíz del repo:** 36 bitácoras `.md` → `docs/archive/sessions/`; `check-salsa-pages.mjs`, `list-projects.mjs`, `PHASE3_RLS_AUDIT_QUERIES.sql` → `scripts/`; iteraciones viejas de logos MIRA → `docs/archive/design-iterations/`. En raíz quedan solo `CLAUDE.md`, `ARCHITECTURE.md`, `MONOREPO.md`.

**`~/.claude` + symlink:** borrado el historial duplicado `projects/-Users-carlosjacoste-Desktop-Claude` (1.5 GB) y el symlink `Desktop/Claude` (causa del duplicado). Memoria persistente intacta (vive bajo la ruta real `-Claude-nosync`). Rutas corregidas en `dev-open.sh` y skills `investor-deck`, `sf-digital-audit`, `repaso-dg`.

## Estado real por cliente (verificado en BD, 2026-07-28)

| Cliente | MIRA | sf-cms | Web en producción |
|---|---|---|---|
| Salsa Burgers | ✅ el más poblado | ✅ completo (4 pág + 4 posts published) | salsaburgers.com desde CMS (build-time bake) |
| Dadybox | ✅ marca/pilares (sin entregables, decisión: no migrar) | — | — |
| Discoolver | ✅ bien poblado | Solo 1 landing espejo | Deploys estáticos propios + discoolver-cms propio |
| Startup Factory | ✅ | (sitio propio SF: 15 pág) | startupsfactory.es (apps/startup-factory-web) |
| NC Global | ✅ contenido mínimo | ⚠️ 4 páginas TODAS en draft, 0 posts | ncglobalassets.com aún desde la SPA Vite legacy |
| Adrian Grooves | ⚠️ casi vacío (solo brand_profile) | ✅ 1 página | Preview vercel.app |
| Lidar Home | ❌ no existe | ❌ no existe | — (solo deliverables locales) |

## Pendientes (decisiones/trabajo futuro)

- **(a) NC Global:** publicar los 4 drafts del CMS + cutover de dominio a `nc-global-assets-next`; después descomisionar el proyecto Vercel legacy.
- **(b) Lidar Home:** alta en MIRA y CMS (decisión CEO: "lo crearemos a futuro").
- **(c) Adrian Grooves:** cargar contenido en MIRA (hoy casi vacío).
- **(d) sf-reports:** carpeta `nc-global/` vacía — hueco por rellenar.
- **(e) Ramas git no mergeadas** (8, revisar si siguen vivas): `dev`, `feature/admin-clients-dashboard`, `feature/admin-dashboard-complete`, `feature/client-portal`, `feature/client-portal-data-wiring`, `feature/tier-schema`, `fix/mira-restore-routes-and-admin`, `tiktok-pixel`.
- **(f) tools/ con espacios:** `10 SKILLS claude/`, `Claude Budget/` — renombrar a kebab-case cuando no haya referencias activas.
- **(g) ai-agency-sf-next ↔ MIRA:** solapamiento funcional; candidata a absorción por MIRA a medio plazo.
- **(h) Salsa en CMS:** los 4 posts tienen `published_at` a null pese a status=published.
- **(i) skill mk-content:** referencia prompts del antiguo agency-os que ya no existen en el monorepo — recrear o actualizar la skill antes de usarla.
- **(j) tools/content-pillars:** solapa conceptualmente con la skill `mk-content` — decidir cuál es la canónica.
- **(k) Discoolver en sf-cms:** decisión de arquitectura pendiente (hoy usa CMS propio + estáticos).
- **(l) Binarios pesados en git** (~210 MB de `.git`): fuente Arial-Unicode.ttf 22 MB, vídeos de Salsa, PDFs — considerar Git LFS si el repo sigue creciendo.
